/**
 * The service screen draws its own FAB.
 *
 * On most screens the brick action is "log an expense" and it lives in this
 * slot, which is a sibling of the page. Here it is "add a schedule item", and
 * the sheet it opens is part of the screen's own state. Same arrangement as the
 * mod board.
 */
export default function ServiceFabSlot() {
  return null
}
